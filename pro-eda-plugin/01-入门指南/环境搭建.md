# 环境搭建

> 配置您的嘉立创EDA插件开发环境

## 📋 环境要求

### 必需软件

- **Node.js**: >= 16.x (推荐 18.x LTS)
- **npm**: >= 8.x (或 yarn/pnpm)
- **嘉立创EDA专业版**: 最新版本

### 推荐软件

- **VS Code**: 推荐的IDE
- **Git**: 版本控制

---

## 🚀 安装 Node.js

### Windows

1. 访问 [Node.js官网](https://nodejs.org/)
2. 下载 LTS 版本
3. 运行安装程序
4. 验证安装：

```bash
node --version
npm --version
```

### macOS

使用 Homebrew:

```bash
brew install node
```

### Linux

```bash
# Ubuntu/Debian
sudo apt install nodejs npm

# CentOS/RHEL
sudo yum install nodejs npm
```

---

## 💻 安装 VS Code

1. 访问 [VS Code官网](https://code.visualstudio.com/)
2. 下载并安装
3. 安装推荐扩展：
    - ESLint
    - Prettier
    - TypeScript Vue Plugin (如使用Vue)

---

## 📦 创建项目

### 1. 创建项目目录

```bash
mkdir my-eda-plugin
cd my-eda-plugin
```

### 2. 初始化 npm 项目

```bash
npm init -y
```

### 3. 安装 TypeScript

```bash
npm install --save-dev typescript @types/node
```

### 4. 安装 API 类型定义

```bash
npm install @jlceda/pro-api-types
```

### 5. 创建 TypeScript 配置

创建 `tsconfig.json`:

```json
{
	"compilerOptions": {
		"target": "ES2020",
		"module": "commonjs",
		"outDir": "./dist",
		"rootDir": "./src",
		"strict": true,
		"esModuleInterop": true,
		"skipLibCheck": true,
		"forceConsistentCasingInFileNames": true,
		"declaration": true,
		"declarationMap": true,
		"sourceMap": true
	},
	"include": ["src/**/*"],
	"exclude": ["node_modules", "dist"]
}
```

---

## 📁 项目结构

创建以下目录结构：

```
my-eda-plugin/
├── src/
│   └── index.ts          # 入口文件
├── dist/                 # 编译输出(自动生成)
├── assets/              # 资源文件
│   └── icon.png         # 插件图标
├── locales/             # 多语言(可选)
│   ├── zh-CN.json
│   └── en-US.json
├── eext.json            # 扩展配置文件
├── package.json
├── tsconfig.json
└── README.md
```

---

## ⚙️ package.json 配置

更新 `package.json`:

```json
{
	"name": "my-eda-plugin",
	"version": "1.0.0",
	"description": "我的第一个EDA插件",
	"main": "dist/index.js",
	"scripts": {
		"build": "tsc",
		"watch": "tsc --watch",
		"dev": "tsc --watch"
	},
	"keywords": ["jlceda", "eda", "plugin"],
	"author": "Your Name",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.0.0",
		"typescript": "^5.0.0"
	},
	"dependencies": {
		"@jlceda/pro-api-types": "latest"
	}
}
```

---

## ✅ 验证环境

运行以下命令验证环境：

```bash
# 检查 Node.js
node --version

# 检查 npm
npm --version

# 检查 TypeScript
npx tsc --version

# 编译项目
npm run build
```

---

## 🎉 完成！

环境搭建完成！接下来：

👉 [核心概念](./核心概念.md)

---

<p align="center">
  <a href="./README.md">← 返回入门指南</a> • 
  <a href="./核心概念.md">下一章 →</a>
</p>
